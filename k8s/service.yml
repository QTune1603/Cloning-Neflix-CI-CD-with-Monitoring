apiVersion: v1
kind: Service
metadata:
  name: neflix-service
  labels:
    app: neflix
spec:
  type: NodePort # For open port
  selector:
    app: neflix # Direct traffic to Pods labeled with the app: Netflix
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007